{% set showMenu = showMenu ?? false %}
{% set moduleName = 'Conformance'|trans %}
{% set subPageTitle = subPageTitle ?? '' %}
{% set pagetitle = 'Conformance' ~ (subPageTitle ? ' | ' ~ subPageTitle : '')  %}

{% extends "base.twig" %}

{% block preload %}
    <link rel="stylesheet" href="{{ asset('css/src/default.css', 'conformance') }}">
{% endblock %}

{% block content %}

    <h2>{{ moduleName }}</h2>

    <div class="conformance-container">

        {% if showMenu %}
            {% include '@conformance/includes/menu.twig' %}
        {% endif %}

        <div class="content">
            <h3>{{ subPageTitle }}</h3>

            {% if genericStatus|default and genericStatus.status is not null %}
                <div class="code-box">
                    <div class="code-box-content">
                        <pre id="" class="">{{ genericStatus.status|escape|upper }}: {{ genericStatus.message|escape }}</pre>
                    </div>
                </div>
            {% endif -%}

            {% block conformanceContent %}{% endblock %}
        </div>
    </div>


{% endblock content -%}

{% block postload %}
    <script>
        (function() {
            window.conformance = {};
            window.conformance.nuclei = {};
        })();
    </script>
{% endblock postload %}

{% block conformancePostload %}{% endblock %}
